package me.smartbde.sml.commonutils;

public class PluginConsts {
    public enum Input {
        CONSOLE,
        TCP,
        HTTP,
        KAFKA
    }

    public enum Filter {

    }

    public enum Output {
        JDBC,
        MONGO
    }
}
